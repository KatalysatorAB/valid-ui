export const VALID_API_URL = 'https://app-api.geposit.se';
export const VALID_API_VERSION = 'v2.0';

export enum ValidEndpoint {
  Suggest = 'suggest',
}

export enum ValidQuery {
  Address = 'address',
}
